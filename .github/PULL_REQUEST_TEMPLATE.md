## What does this PR do?

<!-- One or two sentences describing the change. -->

## Checklist

- [ ] `stylua lua/ plugin/` passes with no diff
- [ ] Tests pass: `nvim --headless -u tests/minimal_init.lua -c "PlenaryBustedDirectory tests/ {minimal_init = 'tests/minimal_init.lua'}"`
- [ ] New behaviour is covered by tests (or explain why it can't be)
- [ ] `CHANGELOG.md` updated if this is a user-facing change
- [ ] `PROTOCOL.md` updated if the wire format changed

## Related issues

<!-- Closes #... -->
